#include <iostream>
using namespace std;

int main() {
    int n, m;
    cout << "Enter the two number : ";
    cin >> n >> m;
    int mul = n * m;
    cout << "Mutiplication of Two Number: " << mul << endl;
    return 0;
}